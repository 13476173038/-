package m03.d29.mingxing.dao;

import m03.d29.mingxing.reader.reader;



public interface BizDao {
	int zhen(reader reader);
	int shan(reader reader);
	int gai(reader reader);
	int cha(reader reader);
}
